# Rust build artifacts
target/

# macOS
.DS_Store

# Derived stores are disposable — never committed
*.db

# Knowledge Plane local state (proposals staging, cursors)
.kp/

# Compose deployment: local secrets + the default vault bind mount
# (examples/compose/.env.example is the committed template)
.env
/vault/
