# `kp-config` contract changelog

## v1 — 2026-07-03

- Initial publication: `schema` version pin; `[vault]`, `[index]`,
  `[curio]`, `[zotero]`, `[librarian]`, `[mcp]` tables.
- Rules: unknown keys warn (never fail); secrets only via env/keychain
  indirection.
